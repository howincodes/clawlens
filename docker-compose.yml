services:
  clawlens:
    build: .
    container_name: clawlens
    restart: always
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - clawlens-data:/app/data
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin}
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
      - PORT=3000
      - DB_PATH=/app/data/clawlens.db

volumes:
  clawlens-data:
